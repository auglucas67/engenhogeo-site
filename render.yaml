services:
  - type: web
    name: engenhogeo
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm exec vinext start -- --hostname 0.0.0.0 --port $PORT
    healthCheckPath: /

